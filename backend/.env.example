# Database
DATABASE_URL=mysql://user:pass@localhost:3306/designer_tracker

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-this
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-this
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Monday.com (webhook secret only - API token stored encrypted in DB)
MONDAY_WEBHOOK_SECRET=your-webhook-secret

# SMTP (cPanel)
SMTP_HOST=mail.yourdomain.com
SMTP_PORT=465
SMTP_USER=noreply@yourdomain.com
SMTP_PASS=your-email-password

# App
APP_URL=http://localhost:5000
FRONTEND_URL=http://localhost:3000
NODE_ENV=development
PORT=5000

# Encryption (for Monday API token)
ENCRYPTION_KEY=your-32-byte-hex-encryption-key-here

# Web Push (VAPID keys — generate with: npx web-push generate-vapid-keys)
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@designertracker.com
